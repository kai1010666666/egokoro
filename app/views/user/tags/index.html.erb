<div class="container">
  <div class="row">
    <h1>タグ一覧</h1>
    <h2>Actions --></h2>
    <%= link_to 'Top', root_path %>
    <h2>Tags --></h2>
    <ul>
      <% @tags.each do |tag| %>
        <li>
          <%# タグの名前と、関連付く記事数を表示 %>
          <%= link_to "#{tag.name} ( Illustrations: #{tag.illustrations.count} )", tag_path(tag) %>
        </li>
      <% end %>
    </ul>
  </div>
</div>