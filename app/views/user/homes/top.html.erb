<div class="container">
  <div><%= notice %></div>
  <div class="row">
    <div class="col">
    <h5 class="text-left">ようこそ、えごころへ！</h5>
    <div class="text-left">このサイトは、イラスト投稿サイトになります。</div>
    <div class="text-left">会員でない方もイラスト一覧の閲覧はできますが、</div>
    <div class="text-left">詳細閲覧するには会員登録が必要になります。</div>
    </div>
  </div>
  <div class="row">
    <div><h3>新着</h3></div>
  </div>
  <div class="row">
    <% @illustrations.each do |illustration| %>
      <div>
        <%= link_to illustration_path(illustration.id),data: {"turbolinks"=>false} do %>
          <%= image_tag illustration.get_image(150,150) %>
          <div><%= illustration.title %></div>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="row">
    <div><h3>ランキング上位</h3></div>
  </div>
  <div class="row">
    <% @illustrations_favorite.each do |illustration| %>
      <%= link_to illustration_path(illustration.id),data: {"turbolinks"=>false} do %>
        <%= image_tag illustration.get_image(150,150) %>
        <div><%= illustration.title %></div>
      <% end %>
    <% end %>
  </div>
  <div class="text-right">
    <%= link_to "全てのイラストを見る", illustrations_path %>
  </div>
</div>